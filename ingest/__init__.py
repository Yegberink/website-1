"""ModelBlocks module-directory ingest.

Turns the org's module_* repos (plus community-registered repos) into a single
trusted dataset for Hugo: validated metadata + pre-sanitised README HTML.
Run with:  python -m ingest
"""
__version__ = "0.1.0"
